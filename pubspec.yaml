name: supabase_test
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter:
    path: /Users/maximilianfornacon/projects/supabase/supabase-flutter/packages/supabase_flutter
  flutter_dotenv: ^5.1.0
  get_it: ^7.6.4
  stacked: ^3.4.1
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
